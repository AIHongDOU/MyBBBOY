# Live2D 素材获取说明

> 本文档说明：为了让数字人"小姚"能够开口说话、眨眼、有表情、有神态，下一步将采用 **Live2D 模型驱动** 的技术路线。你（用户）需要提供 Live2D 模型素材，AI 负责把它们接入到前端并驱动动画。

---

## 一、为什么改用 Live2D

之前的"表情帧 + 嘴型同步"方案需要你提供多张表情图（聆听/思考/微笑 + 各自的张嘴帧），素材要求高、且表情僵化。

Live2D 的优势：
- 单张立绘 + 动画参数（嘴、眼、眉毛、脸、身体），即可实时驱动说话、眨眼、表情、呼吸；
- 嘴型、眨眼、表情切换全部由参数控制，非常自然，无需多张位图；
- 社区素材丰富，可免费/低价获取，且支持自定义。

---

## 二、你需要获取的素材（核心交付物）

项目落地需要以下 **3 部分**，请照此准备：

### 1. Live2D 模型本体（.model3.json 文件夹组）—— 必须
一个完整的 Live2D 模型 = 一个文件夹，内含：
| 文件/目录 | 说明 | 是否必须 |
|-----------|------|----------|
| `xxx.model3.json` | 模型入口配置文件 | ✅ 必须 |
| `xxx.moc3` | 模型网格/变形数据 | ✅ 必须 |
| `xxx.physics3.json` | 物理摆动（头发/胸/衣摆等） | 建议 |
| `xxx.exp3.json` | 表情（高兴/发怒/悲伤/惊讶等） | 强烈建议 |
| `.png` 纹理贴图 | 模型皮肤材质 | ✅ 必须 |
| `motions/` | 动作（呼吸、说话、idle 循环） | 建议 |

> 拿到后放到项目目录：`my-ai-companion/demo/live2d/<模型文件夹>/`

### 2. 嘴型数据（用于"开口说话"）—— 必须
- 模型必须包含 **嘴部参数**：`ParamMouthOpenY`（张嘴 / 闭嘴）。
- 这句话的意思是：只要模型能控制"嘴张多大"，AI 就能用说话音频的音量实时驱动它开合，实现逼真的开口说话。
- 大多数现成 Live2D 模型都自带此参数，无需额外准备。

### 3. 表情分组（可选，用于"神态切换"）
- 模型自带 `exp3` 表情（如：开心、思考、倾听、中性）。
- 若模型没有，也可以只靠"嘴+眼+眉毛"参数组合出神态，不强制。

---

## 三、从哪里获取素材（推荐渠道）

### 渠道 A：免费开源模型（最快上手，推荐先用这个）
| 来源 | 说明 | 链接 |
|------|------|------|
| **Live2D 官方示例模型** | 官方免费模型，质量高，含嘴/眼/表情，最适合先跑通 | https://www.live2d.com/en/download/sample-data/ |
| **Live2D samples（GitHub）** | `Live2D/CubismWebSamples` 仓库里的 `Resources` 模型 | https://github.com/Live2D/CubismWebSamples |
| **natsumetal / Steam 等开源人物模型** | 社区发布的免费 Vtuber 模型 | 搜索引擎搜 "Live2D 免费模型 下载" |

> ⚠️ 注意版权：免费模型请确认可商用/可二次使用；正式商用前请阅读其 License。

### 渠道 B：付费 / 定制建模
| 平台 | 说明 |
|------|------|
| **Booth（日本）** | Live2D 模型交易平台，大量精致模型，按需购买 |
| **淘宝 / 闲鱼** | 中文定制 Live2D 建模服务，提供立绘即可建模 |
| **专业建模师** | 若你有自己的角色立绘，可委托建模（含嘴/眼/表情全套参数） |

### 渠道 C：用你的现有立绘定制（推荐，最贴合"小姚"形象）
- 你已有 `数字人素材1.png` / `数字人素材2.png` / `效果图.png` 等立绘。
- 可选：将立绘交给建模师制作 Live2D 模型（约几百元档），生成全套嘴/眼/表情参数。
- 或：先用渠道 A 的免费模型跑通功能，之后再用自己的立绘替换贴图。

---

## 四、获取后如何交给 AI（交付清单）

拿到素材后，请确认并整理成一个文件夹，然后告诉我路径即可。建议结构：

```
live2d/
  xiaoyao/            ← 模型文件夹（可自定义名字）
    xiaoyao.model3.json
    xiaoyao.moc3
    xiaoyao.physics3.json
    xiaoyao.exp3.json
    texture/          ← 贴图
    motions/          ← 动作
```

**交付前请自查（重要）：**
- [ ] `.moc3` 与 `.model3.json` 文件名对应、路径正确；
- [ ] 模型能正常张嘴（有 `ParamMouthOpenY`）—— 这是"开口说话"的关键；
- [ ] 尽量包含 `exp3` 表情（不做神态切换可跳过）；
- [ ] 模型文件总大小不要过大（几百 KB ~ 几 MB 为宜）。

---

## 五、拿到素材后 AI 会做什么（下一步）

1. 在 `demo/` 中引入 Live2D Cubism Web SDK（本地 js），加载 `xxx.model3.json`；
2. 用说话音频音量驱动 `ParamMouthOpenY`，实现开口说话（对齐现有 `--ai-audio-level` 信号源）；
3. 用会话状态驱动表情/神态（聆听 / 思考 / 微笑 / 中性）；
4. 开启眨眼、呼吸、物理摆动等 idle 动画；
5. 替换现在的静态 `avatar.png` 背景角色。

> 素材就绪后告诉我："Live2D 素材已放到 xxx 路径"，我即可开始接入。